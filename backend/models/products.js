class Products {
    constructor(id, nome, tipo, valor, url) {
        this.id = id,
        this.nome = nome,
        this.tipo = tipo,
        this.valor = valor,
        this.url = url
    }
}

module.exports = Products;